require('dotenv').config(); const { REST } = require('@discordjs/rest'); const { Routes } = require('discord-api-types/v10'); (async()=>{ const rest = new REST({ version: '10' }).setToken(process.env.DISCORD_TOKEN); const app = process.env.CLIENT_ID; try{ await rest.put(Routes.applicationCommands(app), { body: [] }); console.log('Cleared all global application commands'); } catch (e){ console.error('Failed to clear global commands', e && e.message); process.exit(1); } })().catch(e=>console.error(e));