# Dockerfile for the bot service
FROM node:20-slim
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --production

# Copy source
COPY . .

# Koyeb uses PORT env var automatically
ENV PORT=4000
EXPOSE 4000

CMD ["node", "scripts/start-all.js"]
